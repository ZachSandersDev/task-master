{
  "name": "task-master",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "build": "rm -rf ./dist && tsc && cp -R ./src/templates ./dist/templates && chmod +x ./dist/index.js",
    "start": "npm run build && node ./dist/index.js",
    "install": "npm run build && npm i -g"
  },
  "bin": {
    "tm": "./dist/index.js"
  },
  "dependencies": {
    "@types/node": "^16.7.1",
    "@types/prompts": "^2.0.14",
    "fs-extra": "^10.0.0",
    "prompts": "^2.4.1",
    "typescript": "^4.3.5"
  },
  "devDependencies": {
    "@types/fs-extra": "^9.0.12"
  }
}
